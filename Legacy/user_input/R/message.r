# Must be entered into R terminal, quit terminal by using q(), start R by typing R
# Takes a string input from user and writes to console

text <- readline(prompt="Enter name: ")

print(paste("Your text: ", text))
